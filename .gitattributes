# 字节精确的测试数据：任何平台的 checkout 都必须保持仓库内形态。
# 起因（CI run 32582790210，2026-08-22）：windows runner 的
# core.autocrlf 把快照 fixture 换成 CRLF，33 个快照测试全部以
# 「内容全同、仅 \r\n」误报失败。二进制 golden 同理标记 -text，
# 防止任何换行归一化碰它。
tests/fixtures/tui-snapshots/** text eol=lf
tests/fixtures/dsh-session/** -text
tests/fixtures/release-manifest.sha256* -text
tests/fixtures/wasm/** -text
# 同类第三次（CI，2026-08-24）：market 签名 fixture 被 windows 检出
# 重写为 CRLF——minisig 验签直接失败，market-package 打包后比 golden
# 长 7 字节（1292 vs 1285）。签名链的 json 钉 eol=lf；minisig 与整棵
# package 树（打包后按总长度断言）标 -text。
tests/fixtures/market-*.json text eol=lf
tests/fixtures/market-*.minisig -text
tests/fixtures/market-package/** -text
