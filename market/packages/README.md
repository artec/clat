# Package staging

Place reviewed, immutable `.clatpkg` release artifacts here before running
`npm run build`. The build copies only files ending in `.clatpkg` to
`dist/packages/`; this README is not deployed.
